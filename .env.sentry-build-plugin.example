# DO NOT commit your real Sentry auth token.
# Copy this file to `.env.sentry-build-plugin` only when you need Sentry source map uploads.
# You can also set this variable in another local-only env file.
SENTRY_AUTH_TOKEN=YOUR_SENTRY_AUTH_TOKEN
